# Files in this allowlist are real .txt/.gui/.gfx script files in
# this game whose parsing currently fails — known parser/grammar gaps.
# spec/integration/parse_smoke_spec.rb treats entries here as expected
# failures; the smoke fails only on UNexpected failures (regressions).
# A previously-failing file that now passes is positive signal — remove
# it from this list.
#
# HOI4 baseline: 2 remaining. Both look like Category D — likely
# won't-fix at the grammar level:
# - WUW_Germany.txt has `base = ´45` (acute accent typo, presumably
#   meant '45 or 45). Specific to one event file; not worth a grammar
#   relaxation.
# - online_accountcreate.gui has a UTF-8 encoding issue. Encoding
#   problem, not a grammar gap.

- events/WUW_Germany.txt
- pdx_online_assets/interface/online_accountcreate.gui
